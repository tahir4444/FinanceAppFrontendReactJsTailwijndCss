name: Frontend CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      htaccess-changed: ${{ steps.check-htaccess.outputs.changed }}
    
    steps:
      - name: 🧾 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🔍 Check if .htaccess Changed
        id: check-htaccess
        run: |
          # Handle case where there's no previous commit (first push)
          if [ $(git rev-list --count HEAD) -eq 1 ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "📝 First commit detected - will deploy .htaccess"
          elif git diff --name-only HEAD~1 HEAD | grep -q "^\.htaccess$"; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "📝 .htaccess file has changed - will deploy it"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "✅ .htaccess unchanged - skipping htaccess deployment"
          fi

      - name: 🛠 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm ci

      - name: ✍️ Build Frontend for Server 2
        run: |
          rm -rf dist/
          echo "VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}" > .env
          echo "VITE_API_URL=${{ secrets.VITE_API_URL }}" >> .env
          echo "FRONTEND_URL=${{ secrets.FRONTEND_URL_2 }}" >> .env
          npm run build
          mkdir -p builds/server2
          cp -r dist/* builds/server2/
          
          # Only copy .htaccess if it has changed
          if [ "${{ steps.check-htaccess.outputs.changed }}" == "true" ]; then
            if [ -f ".htaccess" ]; then
              echo "📝 .htaccess changed - copying new version to build directory"
              cp .htaccess builds/server2/
            else
              echo "❌ .htaccess marked as changed but file not found in repository root"
              exit 1
            fi
          else
            echo "✅ .htaccess unchanged - will preserve existing file on server"
          fi

      - name: 📤 Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-server2
          path: builds/server2/
          retention-days: 1

  deploy-server2:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Download Server 2 Build
        uses: actions/download-artifact@v4
        with:
          name: build-server2
          path: ./build

      - name: 🧹 Clean Target Directory on Server 2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER2_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            TARGET_DIR="${{ secrets.SERVER2_APP_PATH }}"
            
            echo "🧹 Cleaning target directory: $TARGET_DIR"
            
            # Backup .htaccess if it exists and hasn't changed
            HTACCESS_BACKUP=""
            if [ "${{ needs.build.outputs.htaccess-changed }}" != "true" ] && [ -f "$TARGET_DIR/.htaccess" ]; then
              echo "💾 Backing up existing .htaccess (unchanged)"
              HTACCESS_BACKUP="/tmp/.htaccess.backup.$"
              cp "$TARGET_DIR/.htaccess" "$HTACCESS_BACKUP"
              echo "✅ .htaccess backed up to $HTACCESS_BACKUP"
            fi
            
            # Check if directory exists
            if [ -d "$TARGET_DIR" ]; then
              echo "📁 Target directory exists, cleaning contents..."
              
              # Remove contents but keep directory structure
              sudo rm -rf "$TARGET_DIR"/* 2>/dev/null || echo "⚠️ Some files couldn't be removed"
              sudo rm -rf "$TARGET_DIR"/.[^.]* 2>/dev/null || echo "⚠️ Some hidden files couldn't be removed"
              
              # Ensure proper permissions
              sudo chown -R $USER:$USER "$TARGET_DIR" 2>/dev/null || echo "⚠️ Couldn't change ownership"
              sudo chmod -R 755 "$TARGET_DIR" 2>/dev/null || echo "⚠️ Couldn't change permissions"
            else
              echo "📁 Target directory doesn't exist, creating it..."
              sudo mkdir -p "$TARGET_DIR"
              sudo chown -R $USER:$USER "$TARGET_DIR"
              sudo chmod -R 755 "$TARGET_DIR"
            fi
            
            # Store backup path for next step
            echo "HTACCESS_BACKUP=$HTACCESS_BACKUP" >> $GITHUB_ENV
            
            echo "✅ Target directory prepared for deployment"

      - name: 🚀 Deploy All Files to Server 2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER2_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: 'build/*'
          target: ${{ secrets.SERVER2_APP_PATH }}
          strip_components: 1
          rm: false
          overwrite: true
          timeout: 120s

      - name: 🔧 Handle .htaccess File
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER2_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            TARGET_DIR="${{ secrets.SERVER2_APP_PATH }}"
            
            if [ "${{ needs.build.outputs.htaccess-changed }}" == "true" ]; then
              # .htaccess changed - it should already be deployed with build files
              if [ -f "$TARGET_DIR/.htaccess" ]; then
                echo "✅ New .htaccess deployed successfully"
                sudo chmod 644 "$TARGET_DIR/.htaccess"
                sudo chown $USER:$USER "$TARGET_DIR/.htaccess"
                echo "📝 New .htaccess permissions set to 644"
                echo "📄 New .htaccess content:"
                cat "$TARGET_DIR/.htaccess"
              else
                echo "❌ .htaccess was supposed to be deployed but not found"
                exit 1
              fi
            else
              # .htaccess unchanged - restore from backup if it exists
              HTACCESS_BACKUP="/tmp/.htaccess.backup.$"
              if [ -f "$HTACCESS_BACKUP" ]; then
                echo "🔄 Restoring unchanged .htaccess from backup"
                cp "$HTACCESS_BACKUP" "$TARGET_DIR/.htaccess"
                sudo chmod 644 "$TARGET_DIR/.htaccess"
                sudo chown $USER:$USER "$TARGET_DIR/.htaccess"
                rm "$HTACCESS_BACKUP"
                echo "✅ Original .htaccess restored successfully"
                echo "📄 Restored .htaccess content:"
                cat "$TARGET_DIR/.htaccess"
              else
                echo "⚠️ No .htaccess backup found - this might be the first deployment"
                if [ -f "$TARGET_DIR/.htaccess" ]; then
                  echo "✅ .htaccess found in deployment (unexpected but proceeding)"
                  sudo chmod 644 "$TARGET_DIR/.htaccess"
                  sudo chown $USER:$USER "$TARGET_DIR/.htaccess"
                else
                  echo "❌ No .htaccess file available - this might cause issues"
                fi
              fi
            fi

      - name: 🔁 Reload Apache Server 2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER2_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo systemctl reload apache2
            echo "✅ Apache reloaded successfully"

      - name: 🌐 Verify Deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER2_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            TARGET_DIR="${{ secrets.SERVER2_APP_PATH }}"
            
            echo "🔍 Verifying deployment..."
            
            # Check if files were deployed
            if [ -d "$TARGET_DIR" ] && [ "$(ls -A "$TARGET_DIR")" ]; then
              echo "✅ Files deployed successfully"
              echo "📁 Contents (including hidden files):"
              ls -la "$TARGET_DIR"
            else
              echo "❌ Deployment verification failed - directory is empty or doesn't exist"
              exit 1
            fi
            
            # Check for key files
            if [ -f "$TARGET_DIR/index.html" ]; then
              echo "✅ index.html found"
            else
              echo "❌ index.html not found"
              exit 1
            fi
            
            if [ -d "$TARGET_DIR/assets" ]; then
              echo "✅ assets directory found"
            else
              echo "❌ assets directory not found"
              exit 1
            fi
            
            # Specifically check for .htaccess
            if [ -f "$TARGET_DIR/.htaccess" ]; then
              echo "✅ .htaccess file found and deployed"
              echo "📝 .htaccess file size: $(wc -c < "$TARGET_DIR/.htaccess") bytes"
              if [ "${{ needs.build.outputs.htaccess-changed }}" == "true" ]; then
                echo "🆕 .htaccess was updated in this deployment"
              else
                echo "♻️ .htaccess was preserved from previous deployment"
              fi
            else
              echo "⚠️ .htaccess file not found in deployment"
            fi
            
            echo "✅ Deployment verification completed successfully"
            
            # Final health check from server
            echo "🏥 Performing final health check..."
            if curl -sSf --connect-timeout 10 --max-time 30 "${{ secrets.FRONTEND_URL_2 }}" > /dev/null 2>&1; then
              echo "✅ Frontend is accessible at ${{ secrets.FRONTEND_URL_2 }}"
            else
              echo "⚠️ Frontend may not be accessible yet (this is normal for new deployments)"
            fi

  deployment-summary:
    needs: [build, deploy-server2]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: 📊 Deployment Summary
        run: |
          echo "🚀 Frontend Deployment completed!"
          if [ "${{ needs.build.outputs.htaccess-changed }}" == "true" ]; then
            echo "🆕 .htaccess was updated with new changes"
          else
            echo "♻️ .htaccess was preserved (no changes detected)"
          fi